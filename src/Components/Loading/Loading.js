import React from "react";

import "./Loading.css";

const Loading = (props) => {

    return (
        <React.Fragment>
            <span className="loading_text">Loading</span>
            <br/>
        </React.Fragment>
    )

}

export default Loading;